import React from 'react';

/**
 * {{pascalCase name}} Component: Description of the behavior...
 * @param {Props} props - Component parameters such as: ...
 * @returns {JSX.Element}
 */
export function {{pascalCase name}}(props) {
  // -----------------------CONSTS, HOOKS, STATES
  // -----------------------MAIN METHODS
  // -----------------------AUX METHODS
  // -----------------------RENDER
  return <p>{{pascalCase name}}</p>;
}