import { DependencyList, MutableRefObject } from 'react';
interface ScrollPosition {
    x: number;
    y: number;
}
interface ScrollProps {
    prevPos: ScrollPosition;
    currPos: ScrollPosition;
}
declare type ElementRef = MutableRefObject<HTMLElement | undefined>;
interface useScrollPositionOptions {
    deps?: DependencyList;
    targetElement?: ElementRef;
    wait?: number;
}
export declare const useScrollPosition: (effect: (props: ScrollProps) => void, { deps, targetElement, wait }: useScrollPositionOptions) => void;
export default useScrollPosition;
//# sourceMappingURL=useScrollPosition.d.ts.map