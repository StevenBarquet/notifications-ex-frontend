/// <reference types="react" />
import { Props as ReactSelectProps, OptionTypeBase } from 'react-select';
import './Select.scss';
import { TestMetadata } from 'types';
type SelectProps<T extends OptionTypeBase> = ReactSelectProps<T> & {
    testMetadata?: {
        selectButtonDiv?: TestMetadata;
        selectButton?: TestMetadata;
        selectDropdown?: TestMetadata;
    };
};
declare const SelectButton: <T extends OptionTypeBase = {
    value: string;
    label: string;
    isDisabled?: boolean | undefined;
}>({ className, styles, onChange, iconType, testMetadata, ...otherProps }: SelectProps<T>) => JSX.Element;
export default SelectButton;
