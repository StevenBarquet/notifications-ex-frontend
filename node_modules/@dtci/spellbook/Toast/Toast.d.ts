import { ReactNode } from 'react';
import { ToastOptions } from 'react-toastify';
export declare const ReactToast: import("react-toastify").Toast;
export type ToastPosition = 'bottom-right' | 'bottom-left' | 'bottom-center' | 'top-right' | 'top-left' | 'top-center';
export interface ToastProps {
    /** Toast content */
    content: ReactNode;
    /** Toast position */
    position?: ToastPosition;
    /** containerId */
    containerId?: string | number;
    /** toastId */
    toastId?: string | number;
    animate?: boolean;
    autoClose?: number | false;
    closeOnClick?: boolean;
    additionalToastOps?: ToastOptions;
}
declare const Toast: (props: ToastProps) => void;
export default Toast;
