import { FC, ReactNode } from 'react';
import { CarouselProps as CarouselLibProps } from 'react-multi-carousel';
import './Carousel.scss';
import 'react-multi-carousel/lib/styles.css';
import { TestMetadata } from 'types';
export interface CarouselProps extends CarouselLibProps {
    children: ReactNode;
    className?: string;
    type?: 'inmersive' | 'slider' | 'magnified';
    arrows?: boolean;
    infinite?: boolean;
    testMetadata?: {
        wrapper?: TestMetadata;
        buttons?: TestMetadata;
        leftArrow?: TestMetadata;
        rightArrow?: TestMetadata;
    };
}
declare const Carousel: FC<CarouselProps>;
export default Carousel;
