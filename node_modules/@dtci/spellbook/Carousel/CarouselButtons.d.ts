import { FC } from 'react';
import { CarouselInternalState } from 'react-multi-carousel';
import { TestMetadata } from 'types';
export type selectedItemType = {
    selected: number;
};
export interface CarouselButtonsProps {
    goToSlide?: (selectedItem: number) => void;
    carouselState?: CarouselInternalState | undefined;
    onPageChange?: () => void;
    pageCount?: number | undefined;
    testMetadata?: TestMetadata;
}
declare const CarouselButtons: FC<CarouselButtonsProps>;
export default CarouselButtons;
