import { FC } from 'react';
import './CodeBox.scss';
import { TestMetadata } from 'types';
export interface CodeBoxProps {
    code: string;
    testMetadata?: {
        copyButton?: TestMetadata;
        code?: TestMetadata;
    };
}
declare const CodeBox: FC<CodeBoxProps>;
export default CodeBox;
