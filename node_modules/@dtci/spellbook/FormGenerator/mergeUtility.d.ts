import { BaseFieldType, FormSchema } from './types';
declare const mergeUtility: <I extends Record<string, unknown>, N = BaseFieldType>(coreSchema: string | FormSchema<I, N>, ...subSchemas: (string | Pick<FormSchema<I, N>, "fields" | "pages">)[]) => FormSchema<I, N>;
export default mergeUtility;
