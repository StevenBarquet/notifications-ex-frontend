import { AnySchema } from 'yup';
import { BaseFieldType, Field, FieldValidator } from '../types';
declare const required: <I extends Record<string, unknown>, N = BaseFieldType>(field: Field<I, N>, yup: AnySchema, validator?: FieldValidator) => AnySchema;
export default required;
