import { AnySchema } from 'yup';
import { BaseFieldType, Field, FieldValidator } from '../types';
declare const maxlength: <I extends Record<string, unknown>, N = BaseFieldType>(field: Field<I, N>, yup: AnySchema, validator: FieldValidator) => AnySchema;
export default maxlength;
