import { FormSchema } from '../types';
declare const buildSubmitPayloadTestFormSchema: (readOnly?: boolean, disabled?: boolean, includeDefaultValues?: boolean) => FormSchema<Record<string, unknown>>;
export default buildSubmitPayloadTestFormSchema;
