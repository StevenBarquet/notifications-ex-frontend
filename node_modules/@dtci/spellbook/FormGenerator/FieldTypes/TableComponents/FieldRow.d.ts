import { SyntheticEvent } from 'react';
import { BaseFieldType, Fields as FieldsType } from '../../types';
import './FieldRow.scss';
export interface FieldRowProps<I extends Record<string, unknown>, N = BaseFieldType> {
    fields: FieldsType<I, N>;
    name: string;
    onDelete: (e: SyntheticEvent, i: number) => void;
    itemIndex: number;
    item?: any;
    clearable?: boolean;
    bulk?: number;
    readOnly?: boolean;
    isDeleting?: boolean;
}
declare const FieldRow: <I extends Record<string, unknown>, N = BaseFieldType>({ name, fields, clearable, onDelete, itemIndex, bulk, item, readOnly, isDeleting, }: FieldRowProps<I, N>) => JSX.Element;
export default FieldRow;
