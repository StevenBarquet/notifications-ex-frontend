import { ReactNode, SyntheticEvent } from 'react';
declare const FormCancelButton: ({ onClick, className, children, ...otherProps }: {
    onClick: (e: SyntheticEvent) => void;
    className: string;
    children: ReactNode;
}) => JSX.Element;
export default FormCancelButton;
