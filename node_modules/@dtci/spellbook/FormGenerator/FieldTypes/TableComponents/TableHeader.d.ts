import { SyntheticEvent } from 'react';
import { Fields as FieldsType, Field as FieldType, BaseFieldType } from '../../types';
import './TableHeader.scss';
export interface HeaderProps<I extends Record<string, unknown>, N = BaseFieldType> {
    field: FieldType<I, N>;
    fields: FieldsType<I, N>;
    onAdd: (e: SyntheticEvent, numberToAdd: number) => void;
    readOnly?: boolean;
    disabled?: boolean;
}
declare const TableHeader: <I extends Record<string, unknown>, N = BaseFieldType>({ field, fields, onAdd, readOnly, disabled, }: HeaderProps<I, N>) => JSX.Element;
export default TableHeader;
