import { Field, BaseFieldType } from '../types';
import { DeepPartial } from 'react-hook-form';
export declare const useRequired: <I extends Record<string, unknown>, N = BaseFieldType>(field: Field<I, N>, values: DeepPartial<I>) => boolean;
export default useRequired;
