import { Field, BaseFieldType } from '../types';
export declare const useFieldDisabled: <I extends Record<string, unknown>, N = BaseFieldType>(field: Field<I, N>, index?: number) => boolean;
export default useFieldDisabled;
