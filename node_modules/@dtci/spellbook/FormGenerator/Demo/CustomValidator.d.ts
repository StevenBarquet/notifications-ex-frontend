import { AnySchema } from 'yup';
import { BaseFieldType, Field } from '../types';
declare const customvalidator: <I extends Record<string, unknown>, N = BaseFieldType>(field: Field<I, N>, yup: AnySchema) => AnySchema;
export default customvalidator;
