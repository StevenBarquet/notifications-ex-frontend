/// <reference types="react" />
import { BaseFieldType, FieldProps, MultiFieldValues } from '../types';
import './MultiField.scss';
type MultifieldProps<I extends Record<string, unknown>, N = BaseFieldType> = FieldProps<I, N> & {
    option: MultiFieldValues;
    watchedField: unknown;
    testMetadata?: {
        label?: string;
        multiselect?: string;
    };
};
declare const MultiField: <I extends Record<string, unknown>, N>({ field, name, loadName, defaultValue, index, option, testMetadata, watchedField, }: MultifieldProps<I, N>) => JSX.Element;
export default MultiField;
