/// <reference types="react" />
import { ActionProps, BaseFieldType } from '../types';
declare const Bulk: <I extends Record<string, unknown>, N = BaseFieldType>({ field, }: ActionProps<I, N>) => JSX.Element;
export default Bulk;
