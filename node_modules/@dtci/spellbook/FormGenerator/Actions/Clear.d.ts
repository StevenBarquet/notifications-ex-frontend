/// <reference types="react" />
import { BaseFieldType, Field } from 'FormGenerator/types';
export interface FieldsProps<I extends Record<string, unknown>, N = BaseFieldType> {
    fields?: Field<I, N>[];
}
declare const Clear: <I extends Record<string, unknown>, N = BaseFieldType>({ fields, }: FieldsProps<I, N>) => JSX.Element;
export default Clear;
