/// <reference types="react" />
import { PageGeneratorProps } from '../../PageGenerator';
export interface SurfaceGeneratorProps {
    page: string;
    pageName?: string;
}
declare const SurfaceGenerator: ({ customLayouts, customRenditions, customCanvases, children, page, pageName, ...otherProps }: Omit<PageGeneratorProps, 'page'> & SurfaceGeneratorProps) => JSX.Element | null;
export default SurfaceGenerator;
