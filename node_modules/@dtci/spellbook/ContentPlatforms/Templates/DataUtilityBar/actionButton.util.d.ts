import { ReactNode } from 'react';
import { ActionState } from '../../../types';
export declare const getActionLabel: (actionName: string, actionStates?: ActionState[], onlyNumberSelected?: boolean) => ReactNode;
