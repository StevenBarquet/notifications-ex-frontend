import React from 'react';
import './ActionButton.scss';
import { ActionState, ActionType, DataDefinition, TestMetadata } from '../../../types';
export interface DisplayActionButtonProps {
    dataDefinition?: DataDefinition[];
    actionState?: ActionState[];
    selectId?: string;
    testMetadata?: TestMetadata;
    isDisabled?: boolean;
    showSelectedPanel?: boolean;
    onCancel?: () => void;
    onActionStateChange?: (type: ActionType, newActionStates: ActionState[]) => void;
}
declare const DisplayActionButton: React.FC<DisplayActionButtonProps>;
export default DisplayActionButton;
