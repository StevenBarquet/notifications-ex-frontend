import React from 'react';
import './ActionButton.scss';
import { DataDefinition, ActionState, ActionType, TestMetadata } from '../../../types';
export interface SortActionButtonProps {
    dataDefinition?: DataDefinition[];
    actionState?: ActionState[];
    selectId?: string;
    testMetadata?: TestMetadata;
    isDisabled?: boolean;
    onActionStateChange?: (type: ActionType, newActionStates: ActionState[]) => void;
}
declare const SortActionButton: React.FC<SortActionButtonProps>;
export default SortActionButton;
