/// <reference types="react" />
import './StatusType.scss';
import { JourneyStatus } from '../../types';
interface StatusProps {
    status?: JourneyStatus;
}
declare const StatusType: ({ status }: StatusProps) => JSX.Element;
export default StatusType;
