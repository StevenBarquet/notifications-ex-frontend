import { FC } from 'react';
import './SearchBar.scss';
import { ValueType } from 'react-select';
import { SearchResultBase } from '../SearchTypeaheadTemplate/SearchTypeaheadTemplate';
export interface SearchOption {
    value: string;
    label: string;
}
export interface EntityOption extends SearchOption {
    attributeOptions: SearchOption[];
}
export interface SearchBarProps {
    entityOptions: EntityOption[];
    onSearch: (entity: ValueType<SearchOption, false>, query: string, attribute: ValueType<SearchOption, false>) => void;
    onChange?: (entity: ValueType<SearchOption, false>, query: string, attribute: ValueType<SearchOption, false>) => void;
    onSelect?: (selected: SearchResultBase) => void;
    searchResults?: SearchResultBase[];
    onViewMoreClick?: SearchBarProps['onSearch'];
    placeholder?: string;
    isLoading?: boolean;
    initialQuery?: string;
    portal?: HTMLElement;
}
declare const SearchBar: FC<SearchBarProps>;
export default SearchBar;
