import React from 'react';
import { TestMetadata } from 'types';
import './FullPageLoader.scss';
export interface FullPageLoaderProps {
    /** The name of the page or section being loaded */
    pageName: string;
    /** Use a dataUri to ensure the image is available when requested. Alternatively, use a preload tag for the image. */
    image?: string;
    /** The subheader text */
    subHeader?: string;
    /** Test ID's */
    testMetadata?: {
        body?: TestMetadata;
        image?: TestMetadata;
    };
}
declare const FullPageLoader: React.FC<FullPageLoaderProps>;
export default FullPageLoader;
