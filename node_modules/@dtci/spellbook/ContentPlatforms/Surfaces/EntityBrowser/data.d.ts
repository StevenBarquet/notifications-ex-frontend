import { FormSchema } from '../../../FormGenerator/types';
import { TreeEntity } from '../../../TreeBrowser/types';
export declare const getDemoSchema: (selectedItem: TreeEntity, currentLevel: string) => FormSchema<Record<string, unknown>>;
