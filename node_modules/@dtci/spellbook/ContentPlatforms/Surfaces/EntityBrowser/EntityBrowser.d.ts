import { TreeBrowserProps } from '../../../TreeBrowser';
import { TestMetadata } from 'types';
import './EntityBrowser.scss';
import { ReactNode } from 'react';
export interface EntityBrowserProps<I extends Record<string, unknown>> {
    browser: TreeBrowserProps;
    isFetching?: boolean;
    testMetaData?: {
        loader: TestMetadata;
    };
    children?: ReactNode;
}
declare const EntityBrowser: <I extends Record<string, unknown>>({ browser, isFetching, testMetaData, children, }: EntityBrowserProps<I>) => JSX.Element;
export default EntityBrowser;
