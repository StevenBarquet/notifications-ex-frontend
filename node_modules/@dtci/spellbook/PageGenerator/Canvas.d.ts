/// <reference types="react" />
import { CanvasObject } from './types';
export interface CanvasProps {
    canvas: CanvasObject;
}
declare const Canvas: ({ canvas }: CanvasProps) => JSX.Element | null;
export default Canvas;
