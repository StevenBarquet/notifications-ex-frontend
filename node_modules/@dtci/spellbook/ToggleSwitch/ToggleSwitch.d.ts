import { PropsWithChildren } from 'react';
import './ToggleSwitch.scss';
import { TestMetadata } from 'types';
export interface ToggleSwitchProps<T> {
    initialValue?: T;
    className?: string;
    handleSwitchChange: (value: T) => void;
    testMetadata?: TestMetadata;
}
declare const ToggleSwitch: <T extends unknown>({ initialValue, className, children: childrenProp, handleSwitchChange, testMetadata, ...otherProps }: PropsWithChildren<ToggleSwitchProps<T>>) => JSX.Element;
export default ToggleSwitch;
