/// <reference types="react" />
import { RegisterOptions, Path } from 'react-hook-form';
import { FormFieldProps } from '../FormField';
import { InputProps } from '../Input';
import { TestMetadata } from 'types';
export interface FormTextFieldProps<I extends Record<string, unknown>> {
    /** Name for field (Unique in the form) */
    fieldName: Path<I>;
    /** Display name for field */
    labelText?: string;
    /** Tooltip icon and hover text */
    tooltip?: string[];
    /** Display name for field */
    helper?: string;
    /** Class name */
    className?: string;
    /** Mark field required (*) */
    required?: boolean;
    /** Min length of characters required */
    minLength?: number;
    /** Max length of characters required */
    maxLength?: number;
    /** Rules to pass down to control */
    rules?: RegisterOptions;
    /** Additional input props for Input, based on the Input Component */
    inputProps?: Partial<InputProps>;
    /** Sets test id */
    testMetadata?: {
        wrapper?: TestMetadata;
        input?: TestMetadata;
    };
    defaultValue?: unknown;
}
declare const FormTextField: <I extends Record<string, unknown>>({ fieldName, labelText, tooltip, required, rules, inputProps, helper, testMetadata, minLength, maxLength, readOnly, truncate, defaultValue, ...otherProps }: FormTextFieldProps<I> & Partial<FormFieldProps<I>>) => JSX.Element;
export default FormTextField;
