import { FC } from 'react';
import './TreeBrowser.scss';
import { TreeAction, TreeContext, TreeEntity } from './types';
import { BadgeTypes, TestMetadata } from '../types';
export interface TreeBrowserProps {
    data: TreeEntity;
    primaryKey?: string;
    statusKey?: string;
    lengthKey?: string;
    idKey?: string;
    levels?: string[];
    statuses?: Record<string, BadgeTypes>;
    actions?: Record<string, TreeAction>;
    className?: string;
    onClickItem?: (id: string | number, node: TreeEntity, level: string | number) => void;
    testMetadata?: TestMetadata;
    onCreate?: (parentId: string | number, data: TreeEntity, level: number | string) => Promise<TreeEntity | void>;
    activeItemId?: string | number;
    hideHeader?: boolean;
}
export declare const TreeBrowserContext: import("react").Context<TreeContext | Record<string, never>>;
declare const TreeBrowser: FC<TreeBrowserProps>;
export default TreeBrowser;
