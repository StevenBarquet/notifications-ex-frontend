import { ReactElement } from 'react';
import './InfiniteScroll.scss';
import { TestMetadata } from 'types';
export interface InfiniteScrollProps<T> {
    /** unique component id */
    id: string;
    /** all data */
    data: T[];
    /** function to call once reaching displayLimit */
    onLoadMore: () => void;
    /** max length of data to display before calling onLoadMore. If 0 or undefined, onLoadMore won't be called */
    displayLimit?: number;
    /** ReactElement to display when loading more data */
    loader?: ReactElement;
    /** child function called with limitedData  */
    children: (data: T[]) => ReactElement;
    testMetadata?: TestMetadata;
}
declare const InfiniteScroll: <T>({ id, data, onLoadMore, displayLimit, loader, testMetadata, children, ...otherProps }: InfiniteScrollProps<T>) => ReactElement;
export default InfiniteScroll;
