import React from 'react';
import { CancelDrop, Modifiers, KeyboardCoordinateGetter } from '@dnd-kit/core';
import { SortingStrategy } from '@dnd-kit/sortable';
import { ItemStyleTypes, Items } from './types';
export declare function createRange(length: number, initializer?: (index: number) => string): string[];
interface Props {
    adjustScale?: boolean;
    cancelDrop?: CancelDrop;
    columns?: number;
    containerStyle?: React.CSSProperties;
    coordinateGetter?: KeyboardCoordinateGetter;
    getItemStyles?(args: ItemStyleTypes): React.CSSProperties;
    wrapperStyle?(args: {
        index: number;
    }): React.CSSProperties;
    itemCount?: number;
    items?: Items;
    handle?: boolean;
    renderItem?: any;
    strategy?: SortingStrategy;
    modifiers?: Modifiers;
    minimal?: boolean;
    trashable?: boolean;
    scrollable?: boolean;
    vertical?: boolean;
}
export declare const TRASH_ID = "void";
export declare function MultipleContainers({ adjustScale, itemCount, cancelDrop, columns, handle, items: initialItems, containerStyle, coordinateGetter, getItemStyles, wrapperStyle, minimal, modifiers, renderItem, strategy, trashable, vertical, scrollable, }: Props): React.ReactElement;
export {};
