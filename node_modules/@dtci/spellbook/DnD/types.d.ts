import { UniqueIdentifier } from '@dnd-kit/core';
export type Items = Record<UniqueIdentifier, UniqueIdentifier[]>;
export type ItemStyleTypes = {
    value: UniqueIdentifier;
    index: number;
    overIndex: number;
    isDragging: boolean;
    containerId: UniqueIdentifier;
    isSorting: boolean;
    isDragOverlay?: boolean;
};
