/// <reference types="react" />
import { UniqueIdentifier } from '@dnd-kit/core';
import { ContainerProps } from '../../DndContainer';
export default function DroppableContainer({ children, columns, disabled, id, items, style, ...props }: ContainerProps & {
    disabled?: boolean;
    id: UniqueIdentifier;
    items: UniqueIdentifier[];
    style?: React.CSSProperties;
}): React.ReactElement;
