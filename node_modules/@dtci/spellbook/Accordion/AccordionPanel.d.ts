import { FC } from 'react';
import { CollapsiblePanelProps } from '../CollapsiblePanel';
import { TestMetadata } from 'types';
export type Props = Omit<CollapsiblePanelProps, 'onToggleOpen' | 'isOpen' | 'isOpenByDefault'> & {
    /** ID of the panel, used to control open/close */
    panelId: string;
    testMetadata?: TestMetadata;
};
declare const AccordionPanel: FC<Props>;
export default AccordionPanel;
