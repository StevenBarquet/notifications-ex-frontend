import { Response, NextFunction } from 'express';
import { ExtendedRequest } from '../types';
interface UserIDOptions {
    cookieName?: string;
}
export declare const userMiddleware: (options: UserIDOptions) => (req: ExtendedRequest, res: Response, next: NextFunction) => void;
export default userMiddleware;
//# sourceMappingURL=userMiddleware.d.ts.map