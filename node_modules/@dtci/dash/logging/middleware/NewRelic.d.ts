import { Request, Response, NextFunction } from 'express';
import { NewRelic } from '../types';
interface NewRelicOptions {
    /**
     * Instantiated instance of newrelic in a project
     */
    newrelic: NewRelic;
}
export declare const newrelicMiddleware: (options: NewRelicOptions) => (req: Request, res: Response, next: NextFunction) => void;
export default newrelicMiddleware;
//# sourceMappingURL=NewRelic.d.ts.map